<div class="logo">
  <%= image_tag("overtime.png", size: "50") %>
  <h1>Time Tracker</h1>
</div>
<% if current_user %>
  <ul class="custom-nav nav nav-pills">
    <li class="nav-item">
        <%= link_to '<i class="fas fa-home"></i>'.html_safe, root_path, class: "nav-link #{active?(root_path)}" %>
    </li>
    <%= render 'shared/employee_nav_items' if employee? %>
    <%= render 'shared/audit_log_tab' if admin? %>
    <li class="nav-item dropdown ml-md-auto">
      <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown">Options</a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">

      <%= render 'shared/admin_dropdown' if admin? %>

        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>
    </li>
  </ul>
<% end %>
